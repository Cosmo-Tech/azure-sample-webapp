apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    cosmotech.com/service: {{ .Values.name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000